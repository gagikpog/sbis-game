{
  "name": "sbis-game",
  "version": "1.0.0",
  "description": "",
  "main": "calendar.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "minify": "uglifyjs -mc -o calendar.min.js calendar.js",
    "build": "tsc --build && npm run minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsc": "^2.0.4",
    "typescript": "^5.3.3",
    "uglify-js": "^3.17.4"
  }
}
